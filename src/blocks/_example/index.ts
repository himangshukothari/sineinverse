/**
 * EXAMPLE BLOCK - Module Index
 * 
 * This is the main entry point for the block.
 * It exports everything needed by the block registry.
 */

export { manifest, schema } from './schema';
export { default as Block } from './Block';
